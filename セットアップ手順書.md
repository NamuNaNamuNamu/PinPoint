## 1. 空のプロジェクト → vite プロジェクト作成まで
#### git プロジェクト化
```sh
git init
```

#### 【必要あれば】 node バージョンインストール
```sh
nvm install 22

# PinPoint では node 22 をつかう。
# これで nvm use だけで対応バージョンに切り替えられるように。
echo "22" > .nvmrc
```

#### vite プロジェクト作成 (カレントディレクトリに)
```sh
npm create vite@latest . -- --template react-ts
```

コマンド実行後、ユーザー操作が必要
```
◇  Current directory is not empty. Please choose how to proceed:
│  Ignore files and continue
│
◇  Package name:
│  pin-point
│
◇  Install with npm and start now?
│  Yes
│
◇  Scaffolding project in /Users/yu/_Workspace/PinPoint...
│
◇  Installing dependencies with npm...
```

#### 試しにローカルサーバー起動
```sh
npm run dev -- --host
```

## 2. Excalidraw を導入
```sh
npm install @excalidraw/excalidraw
```